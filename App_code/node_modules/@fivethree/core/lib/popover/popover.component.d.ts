import { FivButton } from './../button/button.component';
import { OnInit } from '@angular/core';
import { NavParams, PopoverController } from '@ionic/angular';
export declare class FivPopover implements OnInit {
    navParams: NavParams;
    popoverController: PopoverController;
    buttons: FivButton[];
    constructor(navParams: NavParams, popoverController: PopoverController);
    ngOnInit(): void;
    onItemClicked(i: number): void;
}
