import { ElementRef, Renderer2, EventEmitter } from '@angular/core';
export declare class FivRipple {
    private el;
    renderer: Renderer2;
    private rippleAnimationEvent;
    round: boolean;
    hover: boolean;
    fivClick: EventEmitter<any>;
    clickEvent(event: MouseEvent): void;
    readonly isRound: boolean;
    readonly canHover: boolean;
    constructor(el: ElementRef, renderer: Renderer2);
    ripple(event?: any): void;
    rippleFactory(): void;
    rippleAnimation({ pageX, pageY }: {
        pageX: any;
        pageY: any;
    }): void;
}
