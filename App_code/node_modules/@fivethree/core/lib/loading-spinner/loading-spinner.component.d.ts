import { DomSanitizer } from '@angular/platform-browser';
import { AnimationBuilder } from '@angular/animations';
import { OnInit, ElementRef, ChangeDetectorRef, EventEmitter, Renderer2 } from '@angular/core';
export declare class FivLoadingSpinner implements OnInit {
    private _document;
    _elementRef: ElementRef;
    change: ChangeDetectorRef;
    private builder;
    sanitizer: DomSanitizer;
    private renderer;
    private static diameters;
    private static styleTag;
    private _diameter;
    _strokeWidth: number;
    _value: number;
    fivProgress: EventEmitter<number>;
    fivComplete: EventEmitter<FivLoadingSpinner>;
    determinateCircle: ElementRef;
    mode: 'indeterminate' | 'determinate';
    circleRadius: number;
    _color: string;
    color: string;
    readonly class: string;
    constructor(_document: any, _elementRef: ElementRef, change: ChangeDetectorRef, builder: AnimationBuilder, sanitizer: DomSanitizer, renderer: Renderer2);
    ngOnInit(): void;
    readonly viewBox: string;
    /** The stroke circumference of the svg circle. */
    readonly strokeCircumference: number;
    /** The dash offset of the svg circle. */
    readonly strokeDashOffset: number;
    /** Stroke width of the circle in percent. */
    readonly circleStrokeWidth: number;
    /** Value of the progress circle. */
    value: number;
    diameter: number;
    /** Stroke width of the progress spinner. */
    strokeWidth: number;
    /** Dynamically generates a style tag containing the correct animation for this diameter. */
    private _attachStyleNode;
    /** Generates animation styles adjusted for the spinner's diameter. */
    private _getAnimationText;
    completeIn(duration: number): void;
    setValue(progress: number): any;
    spin(): void;
    stopSpinning(): void;
    setMode(mode: any): void;
}
