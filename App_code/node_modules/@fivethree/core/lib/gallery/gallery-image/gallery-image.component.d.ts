import { OnInit, ElementRef, EventEmitter } from '@angular/core';
import { SafeResourceUrl } from '@angular/platform-browser';
import { AnimationEvent } from '@angular/animations';
import { FivOverlay } from '../../overlay/overlay.component';
export declare class FivGalleryImage implements OnInit {
    src: string | SafeResourceUrl;
    originalSrc: string | SafeResourceUrl;
    index: number;
    image: ElementRef;
    overlay: FivOverlay;
    viewerState: string;
    animationParams: AnimationParams;
    backdropColor: string;
    openTiming: string;
    closeTiming: string;
    willOpen: EventEmitter<FivGalleryImage>;
    didOpen: EventEmitter<FivGalleryImage>;
    didClose: EventEmitter<FivGalleryImage>;
    constructor();
    ngOnInit(): void;
    open(): void;
    close(position: Position): void;
    slideOut(position: Position, src: any): void;
    handleViewerAnimation(event: AnimationEvent): void;
    private getThumbnailPosition;
}
export declare class Position {
    top: number;
    left: number;
    height: number;
    width: number;
    translate?: number;
}
export interface AnimationParams extends Position {
    timing: string;
}
